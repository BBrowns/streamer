{
  "name": "@streamer/desktop",
  "version": "0.1.0",
  "description": "Electron shell for the Streamer web app",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "predev": "npm run build --workspace=@streamer/stream-server",
    "dev": "electron dist/main.js",
    "prestart": "npm run build --workspace=@streamer/stream-server",
    "start": "electron dist/main.js"
  },
  "devDependencies": {
    "electron": "^40.7.0"
  },
  "dependencies": {
    "bonjour-service": "^1.3.0",
    "electron-updater": "^6.8.3"
  }
}
