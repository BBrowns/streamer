{
  "name": "@streamer/desktop",
  "version": "0.1.0",
  "description": "Electron shell for the Streamer web app",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "vendor": "node scripts/vendor-node.js",
    "predev": "npm run build --workspace=@streamer/stream-server && npm run build",
    "dev": "electron dist/main.js",
    "prestart": "npm run build --workspace=@streamer/stream-server && npm run build",
    "start": "electron dist/main.js",
    "test": "node --test src/*.test.js"
  },
  "devDependencies": {
    "electron": "^40.7.0"
  },
  "dependencies": {
    "bonjour-service": "^1.3.0",
    "electron-updater": "^6.8.3"
  }
}
