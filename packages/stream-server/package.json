{
  "name": "@streamer/stream-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Local P2P daemon and bridge for streaming torrents via HTTP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node26 --sourcemap --outfile=dist/index.js --external:node-datachannel --external:webtorrent --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.3.0",
    "@types/webtorrent": "^0.110.2",
    "cors": "^2.8.6",
    "esbuild": "^0.28.1",
    "express": "^5.2.1",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11",
    "webtorrent": "^3.0.21"
  },
  "dependencies": {
    "@sentry/node": "^10.71.0",
    "@streamer/shared": "*",
    "bonjour-service": "^1.3.0",
    "castv2-client": "^1.2.0",
    "express-rate-limit": "^8.5.2"
  }
}
