diff --git a/node_modules/bittorrent-tracker/lib/server/parse-udp.js b/node_modules/bittorrent-tracker/lib/server/parse-udp.js
index f73b649..ca4c9cf 100644
--- a/node_modules/bittorrent-tracker/lib/server/parse-udp.js
+++ b/node_modules/bittorrent-tracker/lib/server/parse-udp.js
@@ -1,4 +1,3 @@
-import ipLib from 'ip'
 import common from '../common.js'
 import { equal } from 'uint8-util'
 
@@ -30,7 +29,7 @@ export default function (msg, rinfo) {
 
     const ip = msg.readUInt32BE(84) // optional
     params.ip = ip
-      ? ipLib.toString(ip)
+      ? ipv4FromUInt32(ip)
       : rinfo.address
 
     params.key = msg.readUInt32BE(88) // Optional: unique random key from client
@@ -59,6 +58,10 @@ export default function (msg, rinfo) {
   return params
 }
 
+function ipv4FromUInt32 (value) {
+  return `${value >>> 24}.${(value >>> 16) & 255}.${(value >>> 8) & 255}.${value & 255}`
+}
+
 const TWO_PWR_32 = (1 << 16) * 2
 
 /**
